<?php

return [
    'provider' => env('LLM_PROVIDER', 'openai'),

    'openai' => [
        'api_key' => env('OPENAI_API_KEY'),
        'base_url' => env('OPENAI_BASE_URL', 'https://api.openai.com/v1'),
        'model' => env('OPENAI_MODEL', 'gpt-4o-mini'),
        'timeout' => (int) env('OPENAI_TIMEOUT', 120),
        'max_tokens' => (int) env('OPENAI_MAX_TOKENS', 2000),
    ],

    /*
    | Сжатие JSON для «Глубокого анализа» перед отправкой в LLM.
    */
    'deep_analysis' => [
        'max_overdue_issue_keys' => (int) env('LLM_DEEP_ANALYSIS_MAX_OVERDUE_KEYS', 10),
    ],

    /*
    | Appended to every analysis system prompt (not shown in the UI).
    */
    'output_constraints' => <<<'PROMPT'
Строгие правила формата ответа (обязательны):
1. Выводи ТОЛЬКО нумерованный список рекомендаций на русском языке (3–5 пунктов, максимум 6).
2. Каждый пункт — одно законченное предложение с точкой в конце. Не обрывай текст на полуслове.
3. Запрещено: вступления, заключения, заголовки, markdown (**жирный**, ##), английский текст, мета-комментарии («Final Polish», «Yes»), вложенная нумерация, пересказ данных, дисклеймеры, упоминание JSON.
4. Не комментируй объём данных, дубликаты и качество комментариев — только действия для HR/руководителя.
5. Если данных мало — всё равно дай короткий список рекомендаций без объяснения почему данных мало.

Пример правильного ответа:
1. Провести встречу Engineering и HR для разбора инцидента 28 мая и согласовать план коммуникаций с командой.
2. Назначить 1:1 с сотрудниками, у которых резко упало настроение, в течение двух рабочих дней.
3. Включить ежедневный check-in в обязательный ритуал команды на ближайшие две недели.
PROMPT,

    /*
    | Только для «Глубокого анализа» (wellbeing + Jira).
    */
    'deep_analysis_output_constraints' => <<<'PROMPT'
Строгие правила для глубокого анализа (обязательны):
1. Выводи ТОЛЬКО нумерованный список из 3–6 рекомендаций на русском языке.
2. Каждый пункт — одно законченное предложение с точкой в конце.
3. КАЖДАЯ рекомендация должна опираться на блок employee_delivery_matrix:
   - назови сотрудника;
   - приведи минимум одну цифру из tasks (закрыто, создано, просрочено, открыто, avg_resolution_days) и/или wellbeing (avg_mood, avg_stress, team_support_yes_rate);
   - при просрочках укажи ключи задач из overdue_issues, если они есть в JSON.
4. Сопоставляй нагрузку и самочувствие одного человека в одном пункте, когда есть оба блока.
5. Если у человека tasks = null или mapping_note — один пункт на команду: в Jira у assignee должен быть тот же email, что в профиле сотрудника. Остальные пункты — по wellbeing с цифрами.
6. Запрещено: общие советы без цифр, вступления, markdown, английский, пересказ всего JSON целиком.
PROMPT,
];
