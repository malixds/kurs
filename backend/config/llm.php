<?php

return [
    'provider' => env('LLM_PROVIDER', 'openai'),

    'openai' => [
        'api_key' => env('OPENAI_API_KEY'),
        'base_url' => env('OPENAI_BASE_URL', 'https://api.openai.com/v1'),
        'model' => env('OPENAI_MODEL', 'gpt-4o-mini'),
        // Минимум 300 с: BotHub/тяжёлые модели; устаревший OPENAI_TIMEOUT=120 в env контейнера не режет запрос.
        'timeout' => max(300, (int) env('OPENAI_TIMEOUT', 300)),
        'max_tokens' => (int) env('OPENAI_MAX_TOKENS', 4096),
    ],

    /*
    | Сжатие JSON для «Глубокого анализа» перед отправкой в LLM.
    */
    'deep_analysis' => [
        'max_overdue_issue_keys' => (int) env('LLM_DEEP_ANALYSIS_MAX_OVERDUE_KEYS', 10),
    ],

    /*
    | Appended to every analysis system prompt (not shown in the UI).
    */
    'output_constraints' => <<<'PROMPT'
Строгие правила формата ответа (обязательны):
1. Язык — русский. Структура: 3–5 развёрнутых блоков рекомендаций (максимум 6), БЕЗ нумерации (не пиши «1.», «2.», маркеры списка).
2. Каждый пункт — 3–5 предложений (минимум 80 слов на пункт): не одна строка, а мини-план действий.
3. В начале пункта — заголовок с точным name из employee_wellbeing_summary и всеми доступными метриками этого человека (avg_mood, avg_stress, team_support_yes_rate) в скобках.
4. Интерпретация шкал: avg_mood 1–5 (выше — лучше); avg_stress 1–5 (выше — хуже, больше стресса); team_support_yes_rate в % (ниже — слабее поддержка).
5. Далее в пункте обязательно:
   - краткая оценка риска по цифрам (почему это важно сейчас);
   - 2–3 конкретных шага для HR/руководителя (1:1, срок в рабочих днях, что обсудить, кого подключить);
   - ожидаемый эффект или критерий контроля через 1–2 недели.
6. Приоритет — сотрудники с низким avg_mood (≤2.5), высоким avg_stress (≥3.5) или низким team_support_yes_rate (<60%). Не трать объём на тех, у кого всё стабильно хорошо, пока есть рисковые.
7. ЗАПРЕЩЕНО: абстракции без name («сотрудник», «двое», «команда»), markdown, английский, пересказ всего JSON, общие советы без цифр.
8. Между пунктами — пустая строка. Внутри пункта можно разбить на 2 коротких абзаца.
PROMPT,

    /*
    | Только для «Глубокого анализа» (wellbeing + Jira).
    */
    'deep_analysis_output_constraints' => <<<'PROMPT'
Строгие правила для глубокого анализа (обязательны):
1. Язык — русский. 3–6 развёрнутых блоков рекомендаций БЕЗ нумерации (не пиши «1.», «2.»).
2. Каждый пункт — 4–6 предложений (минимум 90 слов): связка wellbeing + задачи из трекера.
3. Источник — employee_delivery_matrix. В начале пункта: точный name и метрики в скобках (avg_mood, avg_stress, team_support_yes_rate; закрыто/просрочено/открыто; при наличии — ключи overdue_issues).
4. Шкалы: avg_mood выше — лучше; avg_stress выше — хуже; team_support_yes_rate в %.
5. В пункте обязательно: оценка риска (настроение + нагрузка/просрочки), 2–3 шага (1:1, перераспределение задач с номерами, сроки), что проверить через неделю.
6. Один пункт = один человек (исключение: mapping_note / unmapped — отдельный короткий пункт).
7. ЗАПРЕЩЕНО: без name, markdown, английский, выдуманные задачи и цифры, общие советы без привязки к matrix.
8. Между пунктами — пустая строка.
PROMPT,
];
